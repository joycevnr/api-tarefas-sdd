# Stack

## Alvo (TO-BE)
- TypeScript (Node.js 20+), Fastify, SQLite via Prisma, testes com Vitest.

## Estado atual (AS-IS)
- Projeto recém-criado; nada implementado ainda.

## Arquivos de controle
- package.json, tsconfig.json, prisma/schema.prisma.

## Divergências alvo × real
- Nenhuma ainda. Registrar quando o real começar a diferir do alvo.
